{
  "name": "@codex-gemini/orchestrator-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Orchestrator control plane shared by the web workspace and the VS Code extension.",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test *.test.ts",
    "typecheck": "tsc --noEmit -p ."
  },
  "devDependencies": {
    "@types/node": "22.19.19",
    "typescript": "5.9.3"
  }
}
